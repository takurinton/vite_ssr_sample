{
    "builds": [
        {
            "src": "/dist/(.*)",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/dist/(.*)",
            "dest": "server/entry-server.js"
        }
    ]
}